api:
  config:
    mail-url: http://localhost:8088/mail
spring:
  application:
    name: ProjectOne
  datasource:
    username: postgres
    password: 12345
    url: jdbc:postgresql://34.136.65.106:5432/postgres
    driver-class-name: org.postgresql.Driver
    jpa:
      properties:
        hibernate:
          jdbc:
            lob:
              non_contextual_creation: true
          format_sql: true
      show-sql: true
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  mustache:
    check-template-location: false

server:
  port: 8888

