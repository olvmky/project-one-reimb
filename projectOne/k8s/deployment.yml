---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-sre-deployment
  labels:
    app: sre-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sre-demo
  template:
    metadata:
      labels:
        app: sre-demo
    spec:
      containers:
        - image: olvmky/reimb-api
          imagePullPolicy: Always
          name: sre-demo
          ports:
            - containerPort: 8888
          livenessProbe:
            httpGet:
              path: /hw
              port: 8888
          readinessProbe:
            httpGet:
              path: /hw
              port: 8888
---
apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  selector:
    app: sre-demo
  type: NodePort
  ports:
    - port: 8888
      targetPort: 8888

